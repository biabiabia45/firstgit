<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="myproject.wallet.domain.repository.UserMapper">

    <!-- 查询用户信息通过ID -->
    <select id="findById" parameterType="java.util.UUID" resultType="myproject.wallet.domain.user.entity.User">
        SELECT * FROM user
        WHERE id = #{id}
    </select>

    <!-- 查询所有用户信息 -->
    <select id="findAll" resultType="myproject.wallet.domain.user.entity.User">
        SELECT * FROM user
    </select>

    <!-- 插入用户信息 -->
    <insert id="insert" parameterType="myproject.wallet.domain.user.entity.User">
        INSERT INTO user (id, username, email, password, address)
        VALUES (#{id}, #{username}, #{email}, #{password}, #{address})
    </insert>

    <!-- 更新用户信息 -->
    <update id="update" parameterType="myproject.wallet.domain.user.entity.User">
        UPDATE user
        SET username = #{username},
        email = #{email},
        password = #{password},
        address = #{address}
        WHERE id = #{id}
    </update>

    <!-- 检查用户是否存在 -->
    <select id="existsById" parameterType="java.util.UUID" resultType="boolean">
        SELECT EXISTS(SELECT 1 FROM user WHERE id = #{id})
    </select>

    <!-- 删除用户信息 -->
    <delete id="deleteById" parameterType="java.util.UUID">
        DELETE FROM user
        WHERE id = #{id}
    </delete>

</mapper>
